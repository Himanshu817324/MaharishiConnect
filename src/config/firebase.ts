// React Native Firebase configuration
import auth from '@react-native-firebase/auth';

// Firebase is automatically initialized with the google-services.json file
// No manual initialization needed for React Native Firebase

export { auth };
